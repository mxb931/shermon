apiVersion: v1
kind: Namespace
metadata:
  name: shermon
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shermon-mysql-pvc
  namespace: shermon
spec:
  volumeMode: Filesystem
  # Match pollinghandler-db style; tune this for your cluster if needed.
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shermon-db
  namespace: shermon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shermon-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: shermon-db
        app.kubernetes.io/name: shermon-db
        app.kubernetes.io/instance: shermon-db
    spec:
      containers:
        - name: mysql
          image: mysql:8.4
          imagePullPolicy: IfNotPresent
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shermon-secrets
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              valueFrom:
                secretKeyRef:
                  name: shermon-secrets
                  key: MYSQL_DATABASE
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: shermon-secrets
                  key: MYSQL_USER
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: shermon-secrets
                  key: MYSQL_PASSWORD
          ports:
            - containerPort: 3306
              protocol: TCP
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: mysql-data
          resources:
            requests:
              cpu: 100m
              memory: 500Mi
            limits:
              cpu: "1"
              memory: 1Gi
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: shermon-mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: shermon-db
  namespace: shermon
spec:
  selector:
    app: shermon-db
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shermon
  namespace: shermon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shermon
  template:
    metadata:
      labels:
        app: shermon
    spec:
      # Optional: uncomment if your registry requires credentials.
      # imagePullSecrets:
      #   - name: your-registry-pull-secret
      containers:
        - name: app
          image: docker.artifactory.sherwin.com/tag-pos/shermon:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: MONITOR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: shermon-secrets
                  key: MONITOR_API_KEY
            - name: MONITOR_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: shermon-secrets
                  key: MONITOR_DATABASE_URL
            - name: MONITOR_LOG_DIR
              value: /tmp/shermon/logs
            - name: MONITOR_CORS_ALLOW_ORIGINS
              value: https://shermon.example.com
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 4
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: shermon
  namespace: shermon
spec:
  selector:
    app: shermon
  ports:
    - name: http
      port: 80
      targetPort: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shermon
  namespace: shermon
  annotations:
    # Keep websocket connections stable.
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
spec:
  ingressClassName: nginx
  # Optional: uncomment once TLS secret exists.
  # tls:
  #   - hosts:
  #       - shermon.example.com
  #     secretName: shermon-tls
  rules:
    - host: shermon.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: shermon
                port:
                  number: 80
