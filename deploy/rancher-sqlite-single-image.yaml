apiVersion: v1
kind: Namespace
metadata:
  name: shermon
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shermon-db-pvc
  namespace: shermon
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shermon
  namespace: shermon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shermon
  template:
    metadata:
      labels:
        app: shermon
    spec:
      # Optional: uncomment if your registry requires credentials.
      # imagePullSecrets:
      #   - name: your-registry-pull-secret
      containers:
        - name: app
          image: your-registry/shermon:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: MONITOR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: shermon-secrets
                  key: MONITOR_API_KEY
            - name: MONITOR_DATABASE_URL
              value: sqlite:////data/monitor/monitor.db
            - name: MONITOR_LOG_DIR
              value: /tmp/shermon/logs
            - name: MONITOR_CORS_ALLOW_ORIGINS
              value: https://shermon.example.com
          volumeMounts:
            - name: monitor-db
              mountPath: /data/monitor
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 4
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
      volumes:
        - name: monitor-db
          persistentVolumeClaim:
            claimName: shermon-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: shermon
  namespace: shermon
spec:
  selector:
    app: shermon
  ports:
    - name: http
      port: 80
      targetPort: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shermon
  namespace: shermon
  annotations:
    # Keep websocket connections stable.
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
spec:
  ingressClassName: nginx
  # Optional: uncomment once TLS secret exists.
  # tls:
  #   - hosts:
  #       - shermon.example.com
  #     secretName: shermon-tls
  rules:
    - host: shermon.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: shermon
                port:
                  number: 80
